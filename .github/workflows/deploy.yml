name: Auto Deploy n8n to Vercel

on:
  schedule:
    - cron: '0 */6 * * *' # toutes les 6 heures
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ uKWPSiL5HJbQSHAWmhUmVhgU }}
          vercel-org-id: ${{ bBVDHQzgS7tFRCalcH182STW }}
          vercel-project-id: ${{ prj_SNlrwxNN2ejfsYtQ7jv2SAfkGKUv }}
          working-directory: .
          prod: true
